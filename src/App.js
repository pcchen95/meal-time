import React from "react";
import HomePage from "./pages/HomePage";

const theme = {
  colors: {
    primary: "tomato",
    accent: "yellow",
  },
};

export default function App() {
  return <HomePage />;
}
